import React from "react";
import { Element } from "react-scroll";

const Portfolio = () => {
  return (
    <Element id="portfolio" style={{ height: "100vh", background: "blue" }}>
      Portfolio
    </Element>
  );
};

export default Portfolio;
